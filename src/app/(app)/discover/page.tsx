import { DiscoverPanel } from '@/components/dashboard/discover-panel'

export const metadata = {
  title: 'Discover — Cresco',
  description: 'AI-powered resource discovery for students — real courses, books, tools and communities',
}

export default function DiscoverPage() {
  return (
    <div>
      <DiscoverPanel />
    </div>
  )
}
